// import React from 'react'
// import { useSelector } from 'react-redux'

// const data = useSelector(state => state.requests.data)

// export const jan = () => {
//     return data.filter(el => el.fields['SAVING ACCOUNT'] === 'CASH ACCOUNT').reduce((acc, cv) => acc + parseInt(cv.fields.AMOUNT), 0)
// }
